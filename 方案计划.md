# 毕设方案:RAG 企业级电商知识库问答系统

> 方案确认版 · 2026-09-16 · 项目目录:langchain项目

## 一、项目概述

基于 LangChain 框架开发企业级 RAG(检索增强生成)知识库问答系统。知识库内容为电商平台商品资料(商品详情、规格参数、常见问题等),用户通过浏览器进行知识库管理和问答。系统回答问题时引用知识库内容并展示引用片段来源,支持多用户独立会话与会话持久化。

## 二、技术选型(已确认)

| 层 | 选型 | 说明 |
|---|---|---|
| 后端框架 | FastAPI(Python) | 自带接口文档,支持流式输出 |
| AI 框架 | LangChain 1.x | 文档切片、检索、提示词编排全链路 |
| 大模型 | 本地 Ollama + Qwen3 32B(默认) | 免费、离线可用;预留 DeepSeek API 切换口 |
| 嵌入模型 | bge-m3(本地) | 中文效果好,免费 |
| 向量数据库 | Chroma | 代码可替换,预留 Milvus 升级路径 |
| 业务数据库 | SQLite(SQLAlchemy ORM) | 零安装,预留 PostgreSQL 升级路径 |
| 前端 | Vue 3 + Element Plus + Vite | 主流、界面好、教程多 |
| 认证 | JWT + bcrypt | 企业级标准 |
| Python | 3.12 独立虚拟环境 | 机器全局 3.14 太新,部分库不兼容 |

## 三、系统架构

```
浏览器(Vue3 + Element Plus)
      │  HTTP + SSE流式输出
      ▼
FastAPI 后端
 ├── 用户模块:注册/登录/改密码(JWT认证)  ──┐
 ├── 知识库管理模块(仅admin可见)      ──┼──► SQLite(用户/会话/消息记录)
 ├── 会话模块:多用户独立会话+历史记录 ──┘
 └── RAG问答链路:
      提问 → 查询改写 → 混合检索(BM25+向量) → RRF融合
           → Rerank精排 → 提示词(要求引用) → 流式生成
           → 答案+引用片段
              ▲               ▲               ▲
         Chroma向量库    bge-m3嵌入    Ollama qwen3:32b
```

## 四、功能需求

1. 浏览器知识库管理:创建/删除知识库、上传文档(PDF/Word/Excel/TXT)、查看切片、重新索引
2. 知识库问答:回答引用知识库,显示引用片段及出处
3. 多用户会话管理:每个用户独立会话列表
4. 会话持久化:历史对话存数据库,重新登录可找回
5. 注册/登录/修改密码
6. 管理员 admin/123456 预置,仅 admin 可进知识库管理页
7. 企业级性能优化(见下)
8. 附加功能:检索调试页、相似问题推荐、操作日志、系统仪表盘、RAGAS 评估报告

## 五、企业级优化清单

- 混合检索:BM25 关键词 + 向量语义,RRF 融合
- Rerank 精排(bge-reranker)
- 查询改写:口语化问题改写为检索查询
- 流式输出(SSE):降低首字延迟
- 嵌入缓存:文档未变更不重复计算向量
- 回答缓存:相同问题命中缓存
- 父子文档切片:检索精准 + 回答上下文完整
- 接口限流 + 结构化日志
- RAGAS 自动评估(忠实度、相关性等指标)

## 六、数据库设计概要

- users:用户表(用户名、密码哈希、角色、创建时间)
- sessions:会话表(用户、标题、创建/更新时间)
- messages:消息表(会话、角色、内容、引用片段 JSON、时间)
- knowledge_bases:知识库表(名称、描述、文档数)
- documents:文档表(文件名、类型、状态、切片数、哈希)
- chunks:切片表(文档、内容、序号;向量存 Chroma)

## 七、开发计划

| 阶段 | 内容 |
|---|---|
| 1. 环境搭建 | Python 3.12 venv、Ollama、qwen3:32b、依赖包 |
| 2. 用户系统 | 数据库、注册/登录/改密码 |
| 3. RAG 核心链路 | 解析→切片→向量化→检索→生成→引用 |
| 4. 知识库管理 | 上传文档、管理页面(admin) |
| 5. 聊天前端 | 流式对话、引用展示、会话管理 |
| 6. 优化+答辩准备 | 混合检索、重排、评估、模拟数据、测试 |

## 八、风险与对策

| 风险 | 对策 |
|---|---|
| 答辩现场断网 | 默认本地模型,全程离线可演示 |
| 电商文档表格多 | Excel 用 pandas 处理,PDF 表格单独解析 |
| 显卡显存分配 | 大模型占显存,嵌入模型放 CPU,错开使用 |
| Python 3.14 兼容性 | 项目用 3.12 独立虚拟环境 |

## 九、决策记录(与用户确认)

- 大模型:本地 Ollama Qwen3 32B 默认,DeepSeek API 作切换口(花钱前先报数)
- 知识库数据:使用模拟电商商品数据(30~50 份,多品类)
- 前端:Vue 3 + Element Plus
- 部署:仅本地演示
- 学校要求:无硬性技术规定
